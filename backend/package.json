{
    "name": "server",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "build": "npm install && npm run clean && npm run copy-files",
        "clean": "rimraf dist",
        "copy-files": "copyfiles -E ./**/* dist -e ./node_modules/**/*"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "body-parser": "^2.2.0",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.1.0",
        "mysql": "^2.18.1"
    },
    "devDependencies": {
        "copyfiles": "^2.4.1",
        "rimraf": "^6.0.1"
    },
    "description": ""
}
